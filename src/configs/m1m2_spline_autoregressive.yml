# Should be first line.
command: python train_flow.py -D configs/data/m1m2.yml -F configs/flows/spline_autoregressive.yml -M configs/models/hierarchical_flow.yml -E configs/evaluation/flow_marginals.yml configs/evaluation/flow_marginal_kdeplots.yml configs/evaluation/flow_2d_kde.yml

seed: 42
model:
  lr: 0.01

loader:
  batch_size: 4
trainer:
  min_epochs: 5
flow:
  input_dim: 2
  count_bins: 8