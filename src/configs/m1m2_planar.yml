# Should be first line.
command: python train_flow.py -D configs/data/m1m2.yml -F configs/flows/planar.yml -M configs/models/hierarchical_flow.yml -E configs/evaluation/flow_heatmap_2d.yml configs/evaluation/dataset_kde.yml

seed: 42
model:
  lr: 0.01

loader:
  batch_size: 4

flow:
    input_dim: 2

trainer:
  earlystopping: 
    patience: 1
